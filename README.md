# Run locally

1. Ensure you have Node.js installed (version 12+).
2. In the project root run:

```
npm start
```

3. Open http://localhost:3000 in your browser — the game should load from `index.html`.

If you prefer a different port, set the `PORT` environment variable, e.g. on Windows PowerShell:

```
$env:PORT=4000; npm start
```
# AngryBirdsStage3
Stage 3 Angry Birds: Introducing Constraint
