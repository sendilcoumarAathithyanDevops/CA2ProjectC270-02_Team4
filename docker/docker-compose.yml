services:
  angrybirds:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: angrybirds-game
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped


##done by hong le